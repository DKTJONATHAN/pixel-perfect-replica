# Copy to .env.local for local dev (never commit real keys if you prefer).
# On Cloudflare: Settings → Build → Variables and secrets (required at BUILD time).

VITE_SUPABASE_URL=https://trrcoknzdwidjbejcmcu.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxx
