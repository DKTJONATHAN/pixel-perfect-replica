import { createFileRoute } from "@tanstack/react-router";
import { GraduationCap, Users, CalendarCheck, Wallet, AlertCircle } from "lucide-react";
import { AppShell } from "@/components/AppShell";
import { Card, Badge } from "@/components/UI";
import { useSchool } from "@/context/SchoolProvider";
import { className, fullName, money, prettyDate, timeAgo } from "@/lib/format";
export const Route=createFileRoute("/dashboard")({component:Dashboard});
function Dashboard(){const {db}=useSchool();const students=db.students.filter(s=>s.status==="Active").length;const staff=db.staff.filter(s=>s.status==="Active").length;const present=db.attendance.filter(a=>a.status==="Present").length;const total=db.attendance.length;const collected=db.payments.reduce((a,p)=>a+p.amount,0);return <AppShell title="Dashboard" subtitle={"Welcome to "+db.settings.schoolName+".")}><div className="space-y-6"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={GraduationCap} label="Active students" value={students} note="Currently enrolled"/><Metric icon={Users} label="Active staff" value={staff} note="Teaching & support"/><Metric icon={CalendarCheck} label="Attendance" value={total?Math.round(present/total*100)+"%":"—"} note="Recorded attendance"/><Metric icon={Wallet} label="Fees collected" value={money(collected,db.settings.currency)} note="Recorded payments"/></div><div className="grid gap-6 xl:grid-cols-[1.4fr_1fr]"><Card className="p-0 overflow-hidden"><div className="border-b border-border px-5 py-4"><h2 className="font-display text-lg font-bold">Recent activity</h2><p className="text-sm text-muted-foreground">Latest academy actions</p></div><div className="divide-y divide-border">{db.activity.slice(0,8).map(a=><div key={a.id} className="px-5 py-4"><p className="text-sm"><b>{a.actor}</b> {a.message}</p><p className="mt-1 text-xs text-muted-foreground">{timeAgo(a.at)}</p></div>)}</div></Card><Card><div className="flex items-center gap-2"><AlertCircle className="size-5 text-warning"/><h2 className="font-display text-lg font-bold">Needs attention</h2></div><div className="mt-5 space-y-3"><div className="flex justify-between rounded-lg bg-muted p-3 text-sm"><span>Pending leave</span><b>{db.leave.filter(l=>l.status==="Pending").length}</b></div><div className="flex justify-between rounded-lg bg-muted p-3 text-sm"><span>Students</span><b>{students}</b></div></div></Card></div><Card><div className="mb-4 flex items-center justify-between"><h2 className="font-display text-lg font-bold">Latest students</h2><Badge tone="primary">{db.students.length} total</Badge></div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b border-border text-xs uppercase text-muted-foreground"><th className="px-3 py-3">Student</th><th className="px-3 py-3">Admission</th><th className="px-3 py-3">Class</th><th className="px-3 py-3">Status</th><th className="px-3 py-3">Admitted</th></tr></thead><tbody>{db.students.slice(0,8).map(s=><tr key={s.id} className="border-b border-border/70"><td className="px-3 py-3 font-medium">{fullName(s)}</td><td className="px-3 py-3">{s.admissionNo}</td><td className="px-3 py-3 text-muted-foreground">{className(db,s.classId)}</td><td className="px-3 py-3"><Badge tone={s.status==="Active"?"success":"neutral"}>{s.status}</Badge></td><td className="px-3 py-3 text-muted-foreground">{prettyDate(s.admissionDate)}</td></tr>)}</tbody></table></div></Card></div></AppShell>}
function Metric({icon:Icon,label,value,note}:{icon:any;label:string;value:string|number;note:string}){return <Card><div className="grid size-10 place-items-center rounded-xl bg-primary-soft text-primary"><Icon className="size-5"/></div><p className="mt-4 text-sm text-muted-foreground">{label}</p><p className="font-display text-2xl font-bold">{value}</p><p className="text-xs text-muted-foreground">{note}</p></Card>}
