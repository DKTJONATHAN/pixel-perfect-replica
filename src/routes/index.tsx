import {createFileRoute,useNavigate} from "@tanstack/react-router";import {useState} from "react";import {ShieldCheck,LockKeyhole,ArrowRight} from "lucide-react";import {useSchool} from "@/context/SchoolProvider";import {Button,Input} from "@/components/UI";export const Route=createFileRoute("/")({component:Login});function Login(){const {ready,user,login,loginAs}=useSchool();const nav=useNavigate();const [email,setEmail]=useState("admin@kidright.ac.ke");const [password,setPassword]=useState("password");const [error,setError]=useState("");if(ready&&user)nav({to:"/dashboard"});return <main className="min-h-screen bg-background"><div className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-sidebar p-12 text-sidebar-foreground lg:flex lg:flex-col lg:justify-between"><div><div className="flex items-center gap-3"><div className="grid size-12 place-items-center rounded-2xl gradient-hero"><ShieldCheck/></div><div><b className="font-display text-xl">KidRight</b><p className="text-xs uppercase tracking-widest opacity-60">Academy</p></div></div><div className="mt-28 max-w-xl"><p className="text-sm font-semibold uppercase tracking-widest text-accent">School management</p><h1 className="mt-4 font-display text-5xl font-bold">One workspace for your whole academy.</h1><p className="mt-5 text-sidebar-foreground/65">Students, attendance, grades, fees, staff and payroll in one connected system.</p></div></div><p className="text-sm opacity-50">KidRight Academy · Local demo workspace</p></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-md"><div className="surface-card p-8"><div className="mb-7"><div className="mb-3 grid size-11 place-items-center rounded-xl bg-primary-soft text-primary"><LockKeyhole/></div><h2 className="font-display text-2xl font-bold">Welcome back</h2><p className="text-sm text-muted-foreground">Sign in to your academy workspace.</p></div><form className="space-y-4" onSubmit={e=>{e.preventDefault();const r=login(email,password);if(r.ok)nav({to:"/dashboard"});else setError(r.error??"Sign in failed")}}><Input label="Email address" type="email" value={email} onChange={e=>setEmail(e.target.value)}/><Input label="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<p className="text-sm text-destructive">{error}</p>}<Button className="w-full">Sign in <ArrowRight className="size-4"/></Button></form><div className="mt-6 border-t pt-5"><p className="mb-3 text-xs font-semibold uppercase text-muted-foreground">Demo access</p><div className="grid grid-cols-3 gap-2">{(["admin","teacher","registrar"] as const).map(r=><button type="button" key={r} onClick={()=>{loginAs(r);nav({to:"/dashboard"})}} className="rounded-lg border px-2 py-2 text-xs capitalize hover:bg-muted">{r}</button>)}</div></div></div></div></section></div></main>}